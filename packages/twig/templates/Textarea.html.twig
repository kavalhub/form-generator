{% do element.addClass(['fg-twig-textarea']) %}
{% set error = element.error is not empty ? '<div class="fg-twig-error">' ~ element.displayErrors ~ '</div>' : '' %}
<div class="fg-twig-field">{{ element.render()|raw }}{{ error|raw }}</div>