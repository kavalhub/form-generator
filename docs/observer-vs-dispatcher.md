# Observer и Dispatcher (устарело)

Документ сохранён для истории. Актуальное описание: [element-events.md](element-events.md).

С 3.3+ `ElementObserverInterface` и `attachObserver()` удалены. Используйте `ElementEventDispatcher` и `ElementChangedEvent`.
