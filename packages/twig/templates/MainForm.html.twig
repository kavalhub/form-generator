{% do element.addClass(['fg-twig-form']) %}
{% set parts = [] %}
{% for child in element.all %}
  {% set parts = parts|merge([decorator.decorateChild(child).html]) %}
{% endfor %}
<form{{ element.htmlTrait|raw }}>{{ parts|join('')|raw }}</form>